
# How to run

```
$ git clone 
$ cd
$ npm install
```
Then copy .example.env into .env and fill in the values

```
$ cp .example.env .env
```
Then load the db schema run the app

```
$ mysql < schema.sql
$ npm start
```

